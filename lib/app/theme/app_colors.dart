import 'package:flutter/material.dart';

/// Couleurs de l'application NutriAI
class AppColors {
  // Couleurs principales
  static const Color primary = Color(0xFF4CAF50); // Vert nutritif
  static const Color primaryDark = Color(0xFF388E3C);
  static const Color primaryLight = Color(0xFF81C784);

  // Couleurs secondaires
  static const Color secondary = Color(0xFFFF9800); // Orange énergique
  static const Color secondaryDark = Color(0xFFF57C00);
  static const Color secondaryLight = Color(0xFFFFB74D);

  // Couleurs d'accent
  static const Color accent = Color(0xFF2196F3); // Bleu info
  static const Color error = Color(0xFFF44336); // Rouge erreur
  static const Color success = Color(0xFF4CAF50); // Vert succès
  static const Color warning = Color(0xFFFF9800); // Orange alerte

  // Couleurs de fond
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  static const Color surfaceDark = Color(0xFF424242);

  // Couleurs de texte
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Color(0xFFBDBDBD);
  static const Color textWhite = Colors.white;

  // Couleurs nutritionnelles (pour les cartes de macros)
  static const Color calories = Color(0xFFFF5722);
  static const Color proteins = Color(0xFF2196F3);
  static const Color carbs = Color(0xFFFF9800);
  static const Color fats = Color(0xFF9C27B0);
  static const Color fiber = Color(0xFF4CAF50);
}

